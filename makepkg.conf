# Local makepkg configuration for package signing
# Copy this to ~/.makepkg.conf or use with makepkg --config

# GPG key for signing packages
GPGKEY="A1929394DC6BC602A765495562F091E70A090877"

# Package output settings
PKGDEST=.
SRCDEST=.
SRCPKGDEST=.
LOGDEST=.

# Build options
BUILDENV=(!distcc color !ccache check !sign)
PACKAGER="Kamil 'Novik' Nowicki <novik@axisos.org>"

# Compression settings
COMPRESSGZ=(gzip -c -f -n)
COMPRESSBZ2=(bzip2 -c -f)
COMPRESSXZ=(xz -c -z -)
COMPRESSZST=(zstd -c -z -q -)
COMPRESSLRZ=(lrzip -q)
COMPRESSLZO=(lzop -q)
COMPRESSZ=(compress -c -f)
COMPRESSLZ4=(lz4 -q)
COMPRESSLZ=(lzip -c -f)

# Extension defaults
PKGEXT='.pkg.tar.zst'
SRCEXT='.src.tar.gz'
